package Thursday;

import java.util.HashMap;
import java.util.Iterator;
import java.util.Vector;
import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
/*		// TODO Auto-generated method stub
		LinkedList<Integer> l = new LinkedList<Integer>();
		
		for (int i = 0; i < 10; i++) {
			l.add(i);
		}
		
		System.out.println(l);
		
		Vector<Integer> v = new Vector<>();
		for (int i = 0; i < 10; i++) {
			v.add(i);
		}
		
		for (Ingeter i: v) {
			System.out.println(i);
		}*/
/*		Scanner scanner = new Scanner(System.in);

	    String input = scanner.nextLine();
	    int x = scanner.nextInt();
	    scanner.close();

		System.out.println(FunctionCalls.composition(input, x));*/
		HashMap<String, String> nameAndBody = new HashMap<String, String>();
		HashMap<String, String> nameAndArgument = new HashMap<String, String>();
		String composition;
		int argument;
		
		Scanner scanner = new Scanner(System.in);
		int numberOfFunctions = scanner.nextInt();
		scanner.nextLine();
		for (int i = 0; i < numberOfFunctions; i++) {
			String line = scanner.nextLine();
			String [] functionAndBody = line.split(" = ");
			String [] functionAndArgument = functionAndBody[0].split(" ");
			nameAndBody.put(functionAndArgument[0], functionAndBody[1]);
			nameAndArgument.put(functionAndArgument[0], functionAndArgument[1]);
		}
		composition = scanner.nextLine();
		argument = scanner.nextInt();
		scanner.close();
		
		System.out.println(nameAndBody.get("inc") + " " + nameAndArgument("inc");
	}

}
